import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.leshommes.dhonneur',
  appName: "Les Hommes D'honneur",
  webDir: 'dist/community-finance-tracker/browser'
};

export default config;
